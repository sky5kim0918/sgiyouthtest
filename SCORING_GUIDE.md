# 점수 계산 및 순위 매기기 가이드

## 📊 점수 계산 방식

### 기본 원리

1. **각 문항마다 점수 부여**
   - 좌측 선택지와 우측 선택지 각각에 A, B, C, D 유형별 점수가 설정됨
   - 사용자가 선택한 선택지의 점수가 해당 유형에 누적됨

2. **점수 합산**
   - 모든 문항(10개)의 선택 결과를 합산
   - 각 유형(A, B, C, D)별 총점 계산

3. **최종 유형 결정**
   - 4개 유형 중 **가장 높은 점수**를 받은 유형이 최종 결과
   - 동점인 경우 **우선순위** 적용: **A → B → C → D**

---

## 🎯 예시

### 예시 1: 명확한 우승

**문항별 선택 및 점수:**
- 문항 1: 좌측 선택 → A: +2점
- 문항 2: 우측 선택 → B: +2점
- 문항 3: 좌측 선택 → C: +2점
- 문항 4: 좌측 선택 → A: +2점
- 문항 5: 우측 선택 → B: +2점
- 문항 6: 좌측 선택 → A: +2점
- 문항 7: 우측 선택 → D: +2점
- 문항 8: 좌측 선택 → A: +2점
- 문항 9: 좌측 선택 → A: +2점
- 문항 10: 좌측 선택 → A: +2점

**총점:**
- A: 12점 (2+2+2+2+2)
- B: 4점 (2+2)
- C: 2점
- D: 2점

**결과: A 유형** (12점으로 가장 높음)

---

### 예시 2: 동점인 경우

**문항별 선택 및 점수:**
- 문항 1: 좌측 선택 → A: +2점
- 문항 2: 우측 선택 → B: +2점
- 문항 3: 좌측 선택 → C: +2점
- 문항 4: 좌측 선택 → A: +2점
- 문항 5: 우측 선택 → B: +2점
- 문항 6: 좌측 선택 → A: +2점
- 문항 7: 우측 선택 → B: +2점
- 문항 8: 좌측 선택 → A: +2점
- 문항 9: 좌측 선택 → A: +2점
- 문항 10: 우측 선택 → B: +2점

**총점:**
- A: 10점 (2+2+2+2+2)
- B: 10점 (2+2+2+2+2)
- C: 2점
- D: 0점

**결과: A 유형** (A와 B가 동점이지만, 우선순위에 따라 A 선택)

---

## 🔢 점수 설정 방법

### `src/lib/testData.ts`에서 점수 설정

각 문항의 좌/우 선택지에 점수를 부여합니다:

```typescript
{
  id: 1,
  leftChoice: {
    text: '좌측 선택지',
    scores: { A: 2, B: 0, C: 0, D: 0 },  // 좌측 선택 시 A에 2점
  },
  rightChoice: {
    text: '우측 선택지',
    scores: { A: 0, B: 2, C: 0, D: 0 },  // 우측 선택 시 B에 2점
  },
}
```

### 점수 설정 팁

1. **균형잡힌 분배**
   - 각 유형이 비슷한 확률로 나오도록 점수 분배
   - 예: A, B, C, D 각각 2~3개 문항에서 점수를 받도록 설정

2. **점수 크기**
   - 보통 0, 1, 2점 사용
   - 중요한 문항은 더 높은 점수(3, 4점) 부여 가능

3. **양쪽 모두 같은 점수**
   - 좌/우 모두 같은 유형에 점수를 주면 해당 문항은 항상 그 유형에 점수 추가
   - 예: `{ A: 2, B: 0, C: 0, D: 0 }` (좌/우 모두)

---

## 📈 순위 결정 로직

### 코드 위치: `src/lib/calc.ts`

```typescript
// 1. 각 유형별 총점 계산
const totalScores: Scores = { A: 0, B: 0, C: 0, D: 0 };

// 2. 모든 답변의 점수 합산
answers.forEach((answer) => {
  const scores = answer.choice === 'left' 
    ? question.leftChoice.scores 
    : question.rightChoice.scores;
  totalScores.A += scores.A;
  totalScores.B += scores.B;
  totalScores.C += scores.C;
  totalScores.D += scores.D;
});

// 3. 최고 점수 찾기
let winner: Type = 'A';  // 기본값 (우선순위 1위)
let maxScore = totalScores.A;

// 4. 동점 처리: A→B→C→D 우선순위
const types: Type[] = ['A', 'B', 'C', 'D'];
types.forEach((type) => {
  if (totalScores[type] > maxScore) {  // > (초과)만 체크
    maxScore = totalScores[type];
    winner = type;
  }
});
```

### 동점 처리 규칙

- **A와 B가 동점**: A 선택 (A가 먼저 체크됨)
- **B와 C가 동점**: B 선택
- **C와 D가 동점**: C 선택
- **모두 동점**: A 선택 (기본값)

**이유**: `>` (초과)만 체크하므로, 동점이면 이전에 설정된 값이 유지됨

---

## 🎲 점수 분배 예시 (10문항)

### 균형잡힌 분배 예시

```
문항 1: 좌=A(2), 우=B(2)
문항 2: 좌=B(2), 우=C(2)
문항 3: 좌=C(2), 우=D(2)
문항 4: 좌=D(2), 우=A(2)
문항 5: 좌=A(2), 우=B(2)
문항 6: 좌=B(2), 우=C(2)
문항 7: 좌=C(2), 우=D(2)
문항 8: 좌=D(2), 우=A(2)
문항 9: 좌=A(2), 우=B(2)
문항 10: 좌=B(2), 우=C(2)
```

이렇게 하면 각 유형이 비슷한 확률로 나올 수 있습니다.

---

## 📝 점수 수정 방법

`src/lib/testData.ts` 파일에서 각 문항의 `scores` 값을 수정하세요:

```typescript
scores: { A: 점수, B: 점수, C: 점수, D: 점수 }
```

- 점수는 0 이상의 정수
- 보통 0, 1, 2, 3, 4점 사용
- 각 문항마다 좌/우 선택지의 점수를 설정

---

## ✅ 체크리스트

점수 설정 시 확인사항:

- [ ] 각 유형(A, B, C, D)이 비슷한 확률로 나오도록 점수 분배
- [ ] 총 10문항으로 설정
- [ ] 동점 시 우선순위(A→B→C→D) 이해
- [ ] 점수 합산이 올바르게 되는지 확인
